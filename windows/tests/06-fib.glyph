# tests/06-fib.glyph — recursive Fibonacci
[fib n]
  <if n < 2>
    return n
  else
    return the [fib n - 1] + the [fib n - 2]

(main)
  (for i in 0..10)
    print "fib(" + string(i) + ") =", the [fib i]
